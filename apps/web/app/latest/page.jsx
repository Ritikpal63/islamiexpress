import{apiFetch}from'@/lib/api';import{demoArticles}from'@/lib/mock';import NewsCard from'@/components/NewsCard';
export const metadata={title:'Latest News'};
export default async function Latest(){const d=await apiFetch('/articles?limit=40');const items=d?.data?.length?d.data:demoArticles;return <div className="container listing-page"><div className="page-heading"><span>LIVE UPDATES</span><h1>Latest News</h1><p>Breaking stories and fresh updates from India, the world, business, sports, entertainment and more.</p></div><div className="latest-list">{items.map((a,i)=><NewsCard article={a} variant="horizontal" key={a.id||i}/>)}</div></div>}
