'use client';
import{useEffect,useState}from'react';import{clientApi}from'@/lib/api';import Link from'next/link';import{Bookmark}from'lucide-react';
export default function Saved(){const[items,setItems]=useState([]);const[loading,setLoading]=useState(true);useEffect(()=>{clientApi('/interactions/saved/me').then(d=>setItems(d.data||[])).catch(()=>{}).finally(()=>setLoading(false))},[]);return <div className="container listing-page"><div className="page-heading"><span>MY ACCOUNT</span><h1>Saved News</h1><p>Your reading list across Islami Express.</p></div>{loading?<div className="empty-state">Loading…</div>:items.length?<div className="saved-list">{items.map(a=><article key={a.id}><div><small>{a.category_name}</small><h3><Link href={`/article/${a.slug}`}>{a.title}</Link></h3><p>{a.summary}</p></div><Bookmark/></article>)}</div>:<div className="empty-state"><Bookmark size={42}/><h3>No saved stories yet</h3><p>Login and tap Save on any article to keep it here.</p><Link className="primary-btn" href="/latest">Explore latest news</Link></div>}</div>}
