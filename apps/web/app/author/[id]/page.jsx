import{apiFetch}from'@/lib/api';import NewsCard from'@/components/NewsCard';
export default async function Author({params}){const{id}=await params;const d=await apiFetch(`/public/authors/${id}`);if(!d?.data)return <div className="container trust-page"><div className="empty-state">Author profile not found.</div></div>;const a=d.data;return <div className="container trust-page"><div className="author-profile"><div className="avatar large">{a.name?.[0]}</div><div><span>AUTHOR</span><h1>{a.name}</h1><p>{a.bio||'Islami Express newsroom journalist.'}</p></div></div><div className="section-title"><h2>Latest stories</h2></div><div className="listing-grid">{a.articles.map(x=><NewsCard key={x.id} article={x}/>)}</div></div>}
