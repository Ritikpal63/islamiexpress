import{apiFetch}from'@/lib/api';
export async function GET(){const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const d=await apiFetch('/articles?limit=50',{next:{revalidate:300}});const items=(d?.data||[]).map(a=>`<item><title><![CDATA[${a.title}]]></title><link>${base}/article/${a.slug}</link><guid>${base}/article/${a.slug}</guid><pubDate>${new Date(a.published_at||Date.now()).toUTCString()}</pubDate><description><![CDATA[${a.summary||''}]]></description></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Islami Express</title><link>${base}</link><description>Latest news from Islami Express</description>${items}</channel></rss>`;return new Response(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
